Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

{{- if .Values.arangodb.enabled }}
ArangoDB can be accessed via port {{ .Values.arangodb.service.port }} on the following DNS name from within your cluster:
  {{ include "jalapeno.fullname" . }}-arangodb:{{ .Values.arangodb.service.port }}

{{- if eq .Values.arangodb.service.type "NodePort" }}
ArangoDB can also be accessed on node port {{ .Values.arangodb.service.nodePort }}.
{{- end }}
{{- end }}

{{- if .Values.influxdb.enabled }}
InfluxDB can be accessed via port {{ .Values.influxdb.service.port }} on the following DNS name from within your cluster:
  {{ .Release.Name }}-influxdb:{{ .Values.influxdb.service.port }}

{{- if eq .Values.influxdb.service.type "NodePort" }}
InfluxDB can also be accessed on node port {{ .Values.influxdb.service.nodePorts.http }}.
{{- end }}
{{- end }}

{{- if .Values.grafana.enabled }}
Grafana can be accessed via port {{ .Values.grafana.service.port }} on the following DNS name from within your cluster:
  {{ .Release.Name }}-grafana:{{ .Values.grafana.service.port }}

{{- if eq .Values.grafana.service.type "NodePort" }}
Grafana can also be accessed on node port {{ .Values.grafana.service.nodePorts.grafana }}.
{{- end }}

To get the admin credentials:

  echo "Username: {{ .Values.grafana.admin.user }}"
  echo "Password: $(kubectl get secret {{ .Release.Name }}-grafana-admin -n {{ .Release.Namespace }} -o jsonpath="{.data.GF_SECURITY_ADMIN_PASSWORD}" | base64 --decode)"
{{- end }}

{{- if .Values.kafka.enabled }}
Kafka can be accessed via port 9092 on the following DNS name from within your cluster:
  {{ .Release.Name }}-kafka:9092
{{- end }}

For more information on using Jalapeno, visit:
https://github.com/cisco-open/jalapeno 