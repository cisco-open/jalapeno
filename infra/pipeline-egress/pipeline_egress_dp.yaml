apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: mdt-pipeline-egress-deployment
  labels:
    app: mdt-stack-pipeline-egress
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mdt-pipeline-egress
  template:
    metadata:
      labels:
        app: mdt-pipeline-egress
    spec:
      containers:
      - name: mdt-pipeline-egress
        image: ievoltron/pipeline:v0.1.13@sha256:9cb8bb29c01423efd700b66e7014bf2efa207453ef16fbcbe709b31c284887bb
        volumeMounts:
        - mountPath: /data
          name: mdt-pipeline-egress-data
        - mountPath: /data/config
          name: mdt-pipeline-egress-data-config
      volumes:
      - name: mdt-pipeline-egress-data
        emptyDir: {}
      - name: mdt-pipeline-egress-data-config
        configMap:
          defaultMode: 420
          name: mdt-pipeline-egress-config
