apiVersion: v1
kind: Service
metadata:
  name: openbmpd-np
  namespace: jalapeno-collectors
  labels:
    app: openbmpd
spec:
  type: NodePort
  ports:
    - name: 5000-tcp
      port: 5000
      nodePort: 30555
      targetPort: 5000
  selector:
    app: openbmpd
